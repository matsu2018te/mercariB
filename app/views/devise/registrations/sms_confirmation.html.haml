.single-container
  %header.single-header
    %h1
      = link_to do
        = image_tag("logo.svg", alt:"mercari")

  %main.single-main
    .signup-panel
      %h2.signup-panel__head 電話番号の確認
      = form_for "" do |f|
        .form-group
          = f.label :telephone, "携帯電話の番号"
          = f.telephone_field :telephone, autofocus: true, autocomplete: "email", placeholder: "携帯電話の番号を入力"
          %p 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
        = f.submit "smsを送信する", class: "btn btn__red"
        %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
      = link_to "電話番号の確認が必要な理由"
  = render "devise/shared/footer"
